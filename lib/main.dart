import 'package:flutter/material.dart'; // Importando o material.
import 'my_app.dart'; // Importando arquivo my_app.dart para o uso da classe/Widget MyApp

void main() {
  // Instanciando o Widget MyApp(), que está dentro do arquivo "my_app.dart"
  runApp(const MyApp()); // runApp() - Responsável por rodar o app.
}

